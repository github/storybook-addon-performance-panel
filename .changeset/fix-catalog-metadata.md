---
"@github-ui/storybook-addon-performance-panel": patch
---

Fix package.json metadata for Storybook integration catalog: switch icon from SVG to PNG, remove unrecognized "lit" framework, and add top-level `main`/`types` fields.
