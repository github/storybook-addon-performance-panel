---
"@github-ui/storybook-addon-performance-panel": patch
---

Use Vercel's `portless` for stable local dev URLs (`perf-react.localhost:1355`, `perf-html.localhost:1355`) with cross-navigation sidebar links
