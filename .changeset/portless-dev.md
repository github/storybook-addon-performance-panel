---
"@github-ui/storybook-addon-performance-panel": patch
---

Use Vercel's `portless` for stable local dev URLs (`examples-react.localhost:1355`, `examples-html.localhost:1355`) with cross-navigation sidebar links
